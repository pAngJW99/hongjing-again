# 《再见宏景》项目交接与复用规范

> 新任务开始时先完整阅读本文件，再检查 `docs/index.html` 与当前 Git 状态。本文记录的是长期约定；实际代码与素材文件是实现真相。

## 1. 项目与发布

- 游戏名：**再见宏景**（网页标题也只用这个名字）。
- 正式网址：https://hongjing-again.cn/
- GitHub 仓库：`pAngJW99/hongjing-again`
- GitHub Pages：`main` 分支的 `docs/` 目录。
- 当前核心页面：`docs/index.html`，目前是单页静态游戏。
- 用户已授权：小型修复在本地检查通过后可直接更新 `main`，无需再让用户确认不可见的本地版本；较大功能、流程重构或高风险改动仍先说明范围。每次发布后确认 `pages-build-deployment` 成功。
- 不要覆盖用户无关改动；发布前至少做 JavaScript 语法检查。

## 2. 游戏结构与现有流程

1. 标题页：显示“再见宏景”，进入后创建或恢复旅人。
2. 宏景火车站（教学关）：左右/上下移动；邮戳和星形便签可任意顺序收集；收齐后从候车室门口查看并进入大地图。
3. 宏景大地图：纸质导航界面，不显示可移动主控或 NPC。已解锁地点可点击；未解锁区域用云雾遮挡。
4. 宏景八中教学楼前：遇见林晚星，完成对话后获得住处钥匙和林晚星的纸条。
5. 找新住处：软引导玩家打开右上角菜单中的地图；途中被公园下棋声吸引，经历迷路和老人指路事件。
6. 新住处：先播放黑屏开门剧情，随后进入房间自由探索；骨头玩具可拾取，小狗可查看，其他家具也有环境交互。

## 3. 视觉和体验基准

- 核心风格：温暖、明亮、有生活感的小城像素风；正视或轻俯视，避免过度立体和破败乡村感。
- 使用像素素材时保持 `image-rendering: pixelated`，避免模糊缩放。
- UI 基调：旧车票、复古纸张、绿皮火车配色；不要做成通用现代卡片 UI。
- 字体统一使用项目原始系统字体栈；不要再嵌入之前尝试的手写字体。
- 动效保留，但避免影响移动性能；纸片粒子需要错峰飘动，层级在背景/火车之后、人物之前。
- 环境音乐默认舒缓，不用刺耳蝉鸣；音乐开关放在菜单中。

## 4. 通用场景页面规则

每个可行走场景应包含：

- 当前地点标题、天气与时间、固定菜单入口。
- 主控、方向键、查看键、对话框。
- 无事件时显示一条淡淡的场景旁白；交互结束点击“完成”后回到旁白。
- 靠近可交互物时明确提示“查看/拾取”；离开触发范围后恢复旁白。
- NPC 对话框显示说话人名字与头部头像；头像只裁头肩，人物与 NPC 按脚底 Y 坐标决定前后层级。
- 场景任务只显示当前场景任务，不得被其他地图任务覆盖。
- 菜单固定包含：修改身份、重新开始、进入/退出沉浸模式、地图、收藏册、环境音。
- “地图”用于切换至宏景大地图；大地图本身是地点选择器，不是行走场景。

## 5. 移动、镜头与空气墙

- 键盘支持方向键/WASD；移动端用 Game Boy 式十字键，四个方向图标统一，可长按连续移动。
- 移动时切换正确的上下左右三帧动画；不可用 CSS 残影叠帧。
- 移动端横向大场景允许滚动，镜头采用“安全边缘跟随”：人物接近屏幕左右边缘时才平移地图，避免持续以人物为中心造成眩晕。
- 桌面端各场景的游戏框尺寸和比例保持一致；素材可以在框内缩放或留黑边。
- 碰撞判定使用人物**脚底点**，不要用整张精灵矩形。
- 空气墙采用原始底图百分比坐标，所有坐标必须基于完整、未裁切的底图。
- 推荐优先级：明确阻挡区 > 明确通行区 > 家具阻挡区 > 基础可行走区。
- 用户画碰撞图时约定：
  - 粗蓝色实心块：强制可通行；
  - 粗红色实心块：强制阻挡；
  - 薄红框：家具/障碍物；
  - 绿色框：场景可活动外边界；
  - 黄色框：特殊通道或需重点保留的地面。
- 新住处当前底图坐标系约为 `1773 × 888`；碰撞实现见 `homeWalkAreas`、`homeSolidZones`、`homeUserPassageZones`、`homeUserSolidZones` 与 `isHomeWalkable()`。
- 每次更换底图尺寸或裁切方式，都必须重新校准 transform、交互点、素材摆放和碰撞；禁止拿浏览器裁屏坐标直接当原图坐标。

## 6. 对话、任务与叙事规则

- 教学采用软提示，不持续命令玩家；玩家完成一次动作后清掉教学提示。
- 文案语气自然、温柔、略带生活幽默，不要像系统说明。
- 重要事件可以逐句“继续”；结束按钮改成“完成”，点击后回到待机旁白。
- 不同交互必须各自保留文本，不能串到候车室跳图或其他对象的逻辑。
- 地图解锁应通过名称高亮、云雾退散和任务文案表现，不使用突兀红框。
- 当前关键文案与角色资料以 `docs/index.html` 中的数据为准；修改对白时保持说话人、头像和事件顺序同步。

## 7. 收藏册与存档

- 收藏册分为“物品”和“人物”两部分。
- 物品采用 3×3 可滚动宫格；未获得前不显示具体内容，避免剧透。
- 人物遇见前为灰色剪影，遇见后显示头像和简介；竖屏点击头像后再弹简介，不在列表中铺长文。
- 新获得物品先显示独立收藏弹框，再放入收藏册；弹框背景必须保持当前场景，不可跳回火车站。
- 已有收藏包括邮戳、星形便签、住处钥匙、林晚星的纸条、蓝色玩具骨头等。
- 使用 `localStorage` 自动存档，保存姓名、年龄、地图、位置、朝向、任务进度、收藏和事件状态。
- 新增进度字段时保证旧存档兼容，提供合理默认值；“重新开始”才清除存档。

## 8. 响应式与兼容性

- 重点同时验证桌面横屏、iPhone Safari 与安卓 Chrome/微信内置浏览器。
- 使用动态视口单位时准备兼容回退；不要仅依据 `innerWidth/innerHeight` 一次性判断方向或地图边界。
- 安卓端特别注意：地址栏伸缩、可视视口变化、图片未加载时尺寸为零、滚动容器边界误判。
- 角色和地图资源要在加载完成后再计算镜头；首屏加载不能永远卡在 99%，超时应允许进入且继续后台加载非关键资源。
- 对话、菜单、方向键不得重叠；沉浸模式退出键固定在原菜单位置附近。

## 9. 素材交付建议

- 场景底图：提供完整未裁切 PNG，并注明原始尺寸。
- 动态物件/NPC/收集物：透明背景 PNG，尽量保留紧凑边界。
- 主控动画：按方向分别提供等画布、脚底基准一致的帧；命名清晰。
- 碰撞图：必须覆盖完整底图，不能使用手机截图或裁切预览；优先使用上述颜色约定。
- 新素材接入后，检查透明背景、像素清晰度、层级、移动端尺寸和首次加载体积。

## 10. 新任务接手清单

1. 阅读本文件和 `docs/index.html`。
2. 检查 Git 状态与线上 `main`，确认没有遗漏的本地变更。
3. 明确本次修改对应的场景、存档字段及移动端影响。
4. 修改后做脚本语法检查；涉及坐标则制作或检查碰撞叠图。
5. 未经用户明确同意不发布；获准后提交 `main` 并确认 Pages 部署成功。
